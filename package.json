{
  "name": "playwright-typescript-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for the Conduit demo app: Playwright, TypeScript, Allure",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:failure-demo": "cross-env CONTRACT_FAILURE_DEMO=1 playwright test --grep @failure-demo --retries 0",
    "lint": "eslint src tests playwright.config.ts && prettier --check src tests playwright.config.ts eslint.config.mjs package.json tsconfig.json",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write src tests playwright.config.ts eslint.config.mjs package.json tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "cross-env": "10.1.0",
    "eslint": "10.9.1",
    "eslint-plugin-playwright": "2.11.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.69.0",
    "zod": "4.5.4"
  }
}
